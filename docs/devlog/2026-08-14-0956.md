# 2026-08-14 0956 开发日志

## 机器

- Windows

## 任务

实现 M3：Windows 自动替换链路（保存/读取剪贴板、模拟 Ctrl+C/V、转换、剪贴板恢复、失败气泡提示），并补齐悬浮条不抢焦点（WS_EX_NOACTIVATE）。

## 本次完成

- 新增 [src-tauri/src/selection.rs](src-tauri/src/selection.rs)：完整自动替换流程：
  1. 保存当前剪贴板文本（非文本内容直接放弃，避免破坏剪贴板）；
  2. 模拟 Ctrl+C 复制选中文本，等待 80ms；
  3. 读取剪贴板，与备份一致判定为未选中文本并恢复中止；
  4. 调用 M2 转换函数；
  5. 写入转换结果，模拟 Ctrl+V 粘贴，等待 80ms；
  6. 按配置恢复原剪贴板（默认开启）。
- 选中文本超过 1MB 时不自动替换，仅将结果写入剪贴板并提示手动粘贴。
- 新增依赖：`arboard 3.6.1`（剪贴板）、`enigo 0.6.1`（键盘模拟）、`windows 0.61`（仅 Windows，用于窗口扩展样式）。
- `execute_button` 改为异步命令：传入文本时直接转换（调试/测试用），未传文本时在后台线程执行完整替换，避免阻塞 UI。
- 前端气泡改为展示 Rust 返回的实际提示（成功/失败/降级文案）。
- 实现 F1.12：启动时为悬浮条设置 `WS_EX_NOACTIVATE`，点击按钮不夺取原应用焦点，保证 Ctrl+C/V 仍作用于原应用。
- 键盘快捷键按平台分流：Windows 用 Ctrl，macOS 分支已预留 Cmd（M5 验证）。

## 关键决策

- 复制/粘贴等待固定 80ms，落在需求 3.1 的 50~100ms 区间，后续可在设置页暴露为高级选项（M4）。
- “粘贴失败”仅能可靠捕获 SendInput/enigo 层错误；目标应用收到按键但不响应的情况无法可靠检测，先用 API 层失败 + 剪贴板恢复兜底。
- 非文本剪贴板（图片/文件）在操作前即放弃，避免任何模拟按键破坏用户剪贴板。
- 剪贴板恢复失败时不崩溃，转换结果保留在剪贴板并如实提示。
- 隐私：整个流程文本只存在于内存，不写日志/配置/磁盘。

## 验证

- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：10/10 通过（M2 转换测试回归无影响）。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：成功，生成 `stringcraft.exe` 与 NSIS 安装包。

## 当前状态与下一步

- M3 代码完成并通过编译级验证；真实场景（记事本/浏览器/IDE 选中文本点击按钮）需在桌面环境手动联调。
- 下一步：M4——设置页（快捷键录制、按钮增删/排序/改文字、行数与尺寸、配置持久化）。

## 遗留问题

- 未做真实应用联调：模拟粘贴是否被目标应用接受、原应用焦点保持等需在 Win10/11 常用软件上实测。
- “目标应用不支持模拟粘贴”的降级（结果保留剪贴板 + 提示手动粘贴）目前仅覆盖 API 层失败，真实无响应场景无法可靠检测。
- macOS 侧 Cmd 键位与辅助功能权限留待 M5。
- 本地 `.gitignore`（IDE 自动追加 `.idea/`）与 `.idea/` 个人状态改动不纳入提交，保持工作区现状。
