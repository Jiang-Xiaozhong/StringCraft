# 2026-08-15 0117 开发日志

## 机器

- Windows

## 任务

仅更新需求文档，不开发代码。新增按钮显示/隐藏、工具条宽度自动调整、设置页拖拽排序 3 项需求，需求版本升至 v0.7。

## 本次完成

- [docs/需求方案.md](../需求方案.md) 升级至 v0.7，日期 2026-08-15。
- 新增需求 1：按钮显示/隐藏
  - 每个转换按钮增加 `visible` 配置；
  - 隐藏按钮不显示在悬浮条上、不计入可见行分组；
  - 隐藏按钮仍在设置页保留并置灰，可随时恢复；
  - 按钮总数上限 90 仍按配置中全部按钮计算。
- 新增需求 2：工具条宽度自动调整
  - 工具条宽度根据每行最多可见按钮数动态计算；
  - 随按钮数量、行数、按钮宽度、隐藏状态即时更新；
  - 超出屏幕可用宽度时按钮缩至最小宽度，不做横向滚动。
- 新增需求 3：拖拽排序
  - 设置页取消上/下箭头按钮；
  - 改为鼠标按住后拖到任意位置，支持跨行移动；
  - 松开后即时保存并同步到悬浮条。
- 同步更新产品范围、F1 功能需求、4.2.2 按钮管理、6.3 配置结构、
  8 已确认决策、9.4 第五轮补充、10 核心验收场景、7 开发计划 M5 内容。

## 关键决策

- 隐藏按钮保留在配置和设置页，避免删除后丢失用户配置。
- 工具条宽度优先自动适配内容；只有超出屏幕可用宽度时才压缩按钮宽度。
- 拖拽排序取代逐次上/下移动，减少操作步骤，支持跨行重排。

## 验证

- 本轮仅修改文档，不涉及代码，无需构建/测试。
- 已检查需求文档相关章节与表格，确保新增内容与既有需求一致。

## 当前状态与下一步

- 新需求已定稿，等待用户下一步指令再开始开发。
- macOS 适配继续延后，等待明确指令。

## 遗留问题

- 背景透明度不生效、主题切换不生效仍记录为待修复项。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动仍未纳入提交，保持现状。
