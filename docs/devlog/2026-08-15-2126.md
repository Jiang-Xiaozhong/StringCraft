# 2026-08-15 2126 开发日志

## 机器

- Windows

## 任务

整体复查需求文档与开发日志，找出遗漏需求并直接开发补齐；本次发现并实现“本地调试日志（默认关闭，可开启）”。

## 本次完成

- 需求核对结论：
  - 除 macOS 适配外，Windows 侧需求基本全部落地；
  - 遗漏项：第 5 节“日志：本地调试日志（默认关闭，可开启）”此前仅有文档描述，代码中没有开关与日志实现。
- 新增本地调试日志：
  - 配置新增 `debugLog`（默认 `false`）；
  - 新增 [src-tauri/src/logging.rs](../src-tauri/src/logging.rs)：开启时把运行事件追加写入应用数据目录下 `logs/stringcraft.log`；
  - 记录启动完成、全局快捷键注册成功/失败、配置保存、转换执行、悬浮条位置/宽度保存等事件；
  - 隐私：不记录任何被转换的用户文本。
- 设置页：
  - 通用设置新增“调试日志”开关（默认关闭）及说明文案。
- [docs/需求方案.md](../需求方案.md) 升级至 v0.10，新增 4.2.4 日志开关说明、9.7 补充项与验收场景。

## 关键决策

- 日志只写运行事件，不写用户文本，符合需求 4.6 隐私红线。
- 日志文件放在与配置文件同目录的 `logs/stringcraft.log`，便于排查。
- 时间戳用 Unix 秒，避免为日志功能引入额外时间库依赖。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：12/12 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。
- 启动 debug EXE 冒烟验证 6 秒：进程存活、无 panic。

## 当前状态与下一步

- 进入 BUG 修复与打磨阶段；本地调试日志已补齐，待用户实机验证设置页开关。
- macOS 适配仍延后，等待明确指令。

## 遗留问题

- 日志开关是否写文件需要用户开启后实际确认。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
