# 2026-08-15 2249 开发日志

## 机器

- Windows

## 任务

修复悬浮条按钮说明气泡超出悬浮条窗口、被边框裁掉的问题。

## 本次完成

- 说明气泡改为在悬浮条窗口内完整显示：
  - 气泡宽度按窗口可用宽度收缩，不再固定 140~260px 导致窄窗口溢出；
  - 优先在上方显示，空间不足时翻转到下方，仍不足时贴边夹紧；
  - 上下左右都按窗口内边距 8px 做边界限制。

## 关键决策

- 气泡使用窗口视口作为边界（`window.innerWidth / innerHeight`），而不是屏幕坐标，避免透明小窗口内被边框裁掉。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。
- 启动 debug EXE 冒烟验证 6 秒：进程存活、无 panic。

## 当前状态与下一步

- 待用户实机验证气泡在窄宽度/贴近边缘时的显示效果。
- macOS 适配仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
