# 2026-08-15 2330 开发日志

## 机器

- Windows

## 任务

修复悬浮条下排按钮的说明气泡被错误放置到窗口上方、顶部被裁掉的问题。

## 本次完成

- 修正气泡“上方显示”的边界判断：
  - 上方显示时气泡会向上平移自身高度，此前按“气泡顶部”判断，导致下排按钮的气泡顶部越过窗口上沿；
  - 现在按“平移后的顶部 ≥ 8px 且底部 ≤ 窗口高度 − 8px”判断，空间不足时自动翻转到下方或贴边显示。

## 关键决策

- 上方气泡的位置坐标表示气泡底部，边界判断必须同时校验顶部与底部，与 transform 平移方向一致。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。
- 启动 debug EXE 冒烟验证 6 秒：进程存活、无 panic。

## 当前状态与下一步

- 待用户实机验证下排按钮悬停时气泡完整可见。
- macOS 适配仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
