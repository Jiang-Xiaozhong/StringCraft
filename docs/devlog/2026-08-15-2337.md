# 2026-08-15 2337 开发日志

## 机器

- Windows

## 任务

让按钮说明气泡紧贴鼠标显示，并修复窄工具条下气泡离鼠标过远的问题。

## 本次完成

- 修复上方显示偏移：
  - 此前上方显示把气泡高度多减了一次，导致气泡比鼠标高出一行多；
  - 现在上方显示时气泡底部距离鼠标仅 12px。
- 气泡跟随鼠标：
  - 悬停期间监听 `mousemove`，气泡位置随鼠标实时更新；
  - 仍保持 500ms 后出现，出现后即跟随。

## 关键决策

- 上方气泡的 `y` 坐标表示气泡底部，偏移只减固定 12px 间距，不再重复减气泡高度。
- 跟随鼠标只在悬停状态下更新位置，不影响出现延迟。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。
- 启动 debug EXE 冒烟验证 6 秒：进程存活、无 panic。

## 当前状态与下一步

- 待用户实机验证窄工具条下气泡紧贴鼠标且完整可见。
- macOS 适配仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
