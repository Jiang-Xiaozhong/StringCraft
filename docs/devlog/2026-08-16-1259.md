# 2026-08-16 1259 开发日志

## 机器

- Windows

## 任务

修复设置窗口点击系统关闭按钮后无法再次打开的问题。

## 本次完成

- 设置窗口监听 `CloseRequested` 事件：
  - 点击 Windows 系统关闭按钮时调用 `prevent_close()` 阻止销毁窗口；
  - 改为 `hide()` 隐藏窗口，之后点击悬浮条设置按钮仍可正常打开。

## 关键决策

- 设置窗口与悬浮条窗口一致采用“隐藏代替销毁”，避免窗口对象被销毁后 `open_settings` 找不到窗口。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：15/15 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 修复完成，待用户实机验证：点系统关闭按钮后，再点设置按钮可重新打开。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
