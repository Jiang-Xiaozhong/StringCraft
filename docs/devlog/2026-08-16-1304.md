# 2026-08-16 1304 开发日志

## 机器

- Windows

## 任务

调整设置页“添加按钮”行的默认行为：打开时不预选下拉框，名称与说明留空，等用户选择后再带出。

## 本次完成

- 初始状态：
  - 下拉框不预选，显示“请选择转换功能”占位项；
  - 名称与说明输入框留空。
- 交互调整：
  - 用户从下拉框选择功能后，才自动带出默认名称与说明；
  - 添加按钮成功后，下拉框恢复未选择状态，名称与说明清空。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 行为调整完成，待用户实机确认。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
