# 2026-08-16 1314 开发日志

## 机器

- Windows

## 任务

调整默认按钮：23 个全部保留在按钮管理列表，10 个常用按钮默认显示、其余 13 个默认隐藏；并清空用户自定义配置。

## 本次完成

- 默认配置：
  - 23 个默认按钮全部保留；
  - 10 个常用按钮（`AB`、`ab`、`Ab`、`aB`、`Aa`、`s_`、`camel`、`c_`、`c sp`、`NoSym`）默认显示；
  - 其余 13 个（分隔符替换、换行处理、JSON、人民币转换等）默认隐藏，设置页保留可恢复。
- Rust 与前端默认配置同步更新（`config.rs` / `defaults.ts`）。
- 默认窗口高度调整为单行布局（10 个可见按钮 + 横排操作按钮）。
- 清空用户自定义配置：
  - 将 `%APPDATA%\StringCraft\config.json` 备份为 `config.json.bak-时间戳` 后移除，下次启动按新默认生成。
- [docs/需求方案.md](../需求方案.md) 同步更新 4.3、F1.22、决策与验收场景。

## 关键决策

- 默认显示选择最常用的 10 个大小写/分词类转换，隐藏分隔符替换、换行、JSON 与人民币转换类按钮，用户可随时恢复。
- 清空配置采用“备份后移除”，可恢复但不会影响新默认生成。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：15/15 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 默认按钮与配置已更新，待用户启动应用确认。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
