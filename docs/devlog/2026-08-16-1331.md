# 2026-08-16 1331 开发日志

## 机器

- Windows

## 任务

按用户指定的顺序重排 23 个默认按钮，前 10 个默认显示、其余隐藏；默认单行布局下设置与隐藏按钮横排。

## 本次完成

- 默认按钮顺序按用户提供清单重排（JSON → 驼峰类 → 大小写类 → 分隔符/换行类 → 人民币类）。
- 前 10 个（`JSON`、`camel`、`c_`、`c sp`、`aB`、`AB`、`ab`、`NoSym`、`s_`、`s↵`）默认显示，其余 13 个默认隐藏。
- Rust `default_buttons`、前端 `DEFAULT_BUTTONS` 顺序与可见性同步更新。
- 默认工具条宽度调整为 844，恰好容纳 10 个按钮 + 横排的设置/隐藏按钮，默认显示为 1 行。
- 默认窗口宽度 844、高度 50 同步更新。
- 需求文档 4.3 默认按钮表增加“默认显示”列并按新顺序重排，F1.22、决策与验收场景同步更新。

## 关键决策

- 前 10 个默认显示按钮完全按用户给定的从上到下顺序取前 10 个。
- 默认宽度按“横排操作按钮 + 10 个按钮”计算，保证默认单行时设置/隐藏按钮横排。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：15/15 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 默认按钮顺序与布局已更新，待用户启动应用确认。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
