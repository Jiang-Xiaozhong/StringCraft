# 2026-08-16 1441 开发日志

## 机器

- Windows

## 任务

修复设置页“全局快捷键 呼入/呼出悬浮条”无法自定义的问题。

## 本次完成

- 快捷键录制从“只监听输入框按键”改为监听整个设置窗口的 `keydown`：
  - 点击快捷键输入框进入录制状态后，在窗口任意位置按下组合键都能被捕获；
  - 捕获成功后自动移除监听并保存。
- 增加重复进入录制状态的保护，组件销毁时清理监听。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 快捷键自定义已修复，待用户实机确认。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
