# 2026-08-16 1448 开发日志

## 机器

- Windows

## 任务

分析自定义按钮需求并写入需求文档 v0.14，本轮不改代码。

## 本次完成

- [docs/需求方案.md](../需求方案.md) 升级至 v0.14：
  - 新增自定义按钮类型：加后缀、加前缀、文本替换、去重复行；
  - 参数规则：加后缀/加前缀各 1 个参数；文本替换拆分为“被替换文本 + 替换为文本”2 个参数；去重复行无参数；
  - 自定义按钮添加后默认隐藏（`visible=false`），保留在按钮管理，勾选显示后出现在悬浮条；
  - 支持修改名称、说明与参数；
  - 文本替换为全文区分大小写、非正则替换；去重复行为完整匹配、保留首次出现；
  - 配置结构补充 `customType`、`param1`、`param2`；
  - 新增决策 32~35、9.11 补充项 55~58、验收场景与 M6 内容更新。

## 关键决策

- “文本替换”需求原文只提到一个输入框，但从“aa 替换 bb”的示例看需要同时知道被替换文本，因此拆分为两个输入框，避免歧义。
- 自定义按钮与内置按钮共用 `visible` 机制：默认隐藏，用户勾选后显示。
- 本期文本替换不做正则，仅支持普通文本区分大小写替换。

## 验证

- 本轮仅修改需求文档，不涉及代码，无需构建/测试。
- 已通读检查相关章节与配置结构，内容一致。

## 当前状态与下一步

- 自定义按钮需求已定稿，等待用户确认后开始开发。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 文本替换拆分为两个输入框属于对需求的合理推导，待用户确认。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
