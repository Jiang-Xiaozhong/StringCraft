# 2026-08-16 1455 开发日志

## 机器

- Windows

## 任务

开发自定义按钮需求（加后缀、加前缀、文本替换、去重复行），按用户要求暂不提交，等待测试通过后再提交。

## 本次完成

- 配置模型：
  - `TransformButton` 新增 `customType`、`param1`、`param2`；
  - `validate` 支持自定义按钮参数校验。
- 转换核心：
  - 新增 `transform_custom` 与 4 个自定义转换实现；
  - 加后缀 / 加前缀按行处理，保留换行与 CRLF；
  - 文本替换为全文区分大小写、非正则替换；
  - 去重复行按完整匹配保留首次出现。
- 执行链路：
  - `execute_button` 增加自定义参数透传；
  - 悬浮条点击自定义按钮时携带参数执行。
- 设置页：
  - 新增“添加自定义按钮”区域（类型选择 + 参数输入 + 名称/说明）；
  - 自定义按钮添加后默认隐藏（`visible=false`）；
  - 按钮管理列表支持修改自定义按钮的名称、说明与参数。
- 排版调整：按钮管理与“添加自定义按钮”两处的参数输入框（后缀/前缀/被替换/替换为）统一放到说明输入框后面。
- 设置窗口默认宽度调整为 880，保证背景颜色 10 个色块整行显示。
- 已清空用户个人配置（备份为 `config.json.bak-20260816151343`）。
- 测试：新增自定义转换单元测试（含 CRLF 场景），`cargo test` 16/16 通过。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：16/16 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。

## 当前状态与下一步

- 代码已开发完成但未提交，等待用户测试通过后再提交推送。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
- 所有改动（含本日志）均停留在工作区，未创建任何提交。
