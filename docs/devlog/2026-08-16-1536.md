# 2026-08-16 1536 开发日志

## 机器

- Windows

## 任务

分析并整理检查更新/自动更新、中英文切换、打赏三项需求，写入需求文档 v0.15，本轮不改代码。

## 本次完成

- [docs/需求方案.md](../需求方案.md) 升级至 v0.15：
  - 新增 4.8 检查更新与自动更新（GitHub Releases，手动 + 定时 + 可选自动）；
  - 新增 4.9 多语言（中英文切换，页面文案与默认按钮文案，可扩展更多语言）；
  - 新增 4.10 打赏（支付宝/微信收款码，默认显示、可隐藏并持久化）；
  - 更新 2.1/2.2 产品范围与隐私说明（更新检查为唯一网络请求）；
  - 配置结构新增 `language`、`autoCheckUpdate`、`autoUpdate`、`showDonation`；
  - 新增决策 36~38、9.12 补充项 59~61、验收场景与 M6 内容更新；
  - 后续阶段顺延说明：M6 为当前阶段，M7 Windows 发布准备，M8 macOS 适配（延后），M9 双平台发布。

## 关键决策

- 更新逻辑：
  - 手动检查按钮 + 启动延迟检查一次 + 每 24 小时定时检查；
  - 自动更新默认关闭，开启后自动下载安装包并提示用户确认安装（Windows NSIS）；
  - 更新检查仅访问 GitHub Releases，不上传任何数据，失败静默降级。
- 多语言：语言包结构（zh-CN / en-US）支持后续扩展；默认按钮文案随语言切换，用户自定义内容不动。
- 打赏：收款码从 `assets` 移入前端资源目录随包发布；隐藏选择持久化。

## 验证

- 本轮仅修改需求文档，不涉及代码，无需构建/测试。
- 已通读检查相关章节与配置结构，内容一致。

## 当前状态与下一步

- 三项需求已定稿并纳入 M6，等待用户指令再开始开发。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 自动更新安装细节（静默安装/引导安装）待开发时细化。
- 语言默认值（跟随系统或中文）待实现时确认。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
