# 2026-08-16 1631 开发日志

## 机器

- Windows

## 任务

实现 M6（v0.15）新增需求：检查更新与自动更新、中英文切换、打赏。

## 本次完成

- 检查更新与自动更新：
  - 新增 `update.rs`，基于 GitHub Releases 查询最新版本并比较；
  - 设置页新增“检查更新”按钮、自动检查更新开关（默认开启）、自动更新开关（默认关闭）；
  - 后台启动延迟 10 秒检查一次，之后每 24 小时检查一次；
  - 发现新版本时弹窗提示，自动更新开启时自动下载安装包并提示安装；
  - 新增命令 `check_for_update` / `download_update` / `install_update`。
- 中英文切换：
  - 新增 `language` 配置字段与 `src/lib/i18n.ts` 语言包（zh-CN / en-US）；
  - 设置页主要界面文案、悬浮条气泡消息、托盘菜单随语言切换；
  - 默认按钮名称/说明随语言切换（`getDefaultButtons`），用户自定义内容不受影响。
- 打赏：
  - 收款码从 `assets` 移入 `src/assets/`（alipay.jpg / wechat.jpg）随前端打包；
  - 设置页新增“打赏”区，默认显示支付宝/微信二维码，可隐藏并持久化（`showDonation`）。
- 配置新增：`language`、`autoCheckUpdate`、`autoUpdate`、`showDonation`。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：16/16 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功，收款码已打包进前端资源。

## 当前状态与下一步

- M6（v0.15）三项需求已实现，待用户实机验证（更新检查需要网络与 GitHub Release）。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 更新检查依赖网络，离线时静默失败。
- 自动更新在 Windows 下直接启动安装包，未做静默安装。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
