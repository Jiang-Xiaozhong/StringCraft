# 2026-08-16 1649 开发日志

## 机器

- Windows

## 任务

优化“检查更新”无新版本时的提示文案：明确告知当前版本已是最新。

## 本次完成

- 提示文案改为带版本号的表达：
  - 中文：当前版本 v{version} 已是最新版本；
  - English：You are already on the latest version (v{version})。
- 手动检查更新后无新版本时，用最新版本号填充提示。

## 验证

- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功（构建前已结束残留 StringCraft 进程）。

## 当前状态与下一步

- 更新提示已优化，待用户实机确认。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
