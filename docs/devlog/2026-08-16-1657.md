# 2026-08-16 1657 开发日志

## 机器

- Windows

## 任务

M6 收尾并启动 M7（Windows 打磨与发布准备）：异常处理增强、README 完善、回归清单、体积检查。

## 本次完成

- M6 状态更新：需求文档 M6 标记为已完成，M7 标记为进行中。
- 异常处理增强：
  - 新增 `StartupNotice` 一次性提示状态；
  - 启动时全局快捷键注册失败会在悬浮条加载后弹出气泡提示（不再只写日志）。
- README 重写：补充主要功能、检查更新、隐私、反馈/打赏、目录结构与当前阶段。
- 新增 [docs/测试清单.md](../测试清单.md)：Windows 回归测试清单（启动/托盘、自动替换、悬浮条、设置页、更新、其他）。
- 体积检查：
  - debug EXE 19.83MB（debug 产物，不代表发布）；
  - NSIS 安装包 4.12MB，满足 ≤15MB 目标。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：16/16 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：构建成功。
- 启动 debug EXE 冒烟 6 秒：进程存活、无 panic。

## 当前状态与下一步

- M7 进行中：已启动异常处理、README 与回归清单；下一步按测试清单做 Win10/11 回归，并考虑正式发布打包。
- macOS 适配（M8）仍延后，等待明确指令。

## 遗留问题

- Win10/11 实机回归需要用户按测试清单逐项执行。
- debug EXE 体积不代表发布体积，正式构建后再复核。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
