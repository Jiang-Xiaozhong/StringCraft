# 2026-08-16 2124 开发日志

## 机器

- Windows

## 任务

开始 M8 macOS 适配：辅助功能权限检测与引导、Cmd+Option+Space 快捷键、DMG 打包配置；同步更新需求文档。

## 本次完成

- 新增 `macos.rs`：
  - macOS 下通过 `AXIsProcessTrusted` 检测辅助功能权限；
  - 提供一键打开系统“辅助功能”设置；
  - Windows 下为桩实现，保证跨平台构建。
- 自动替换链路：macOS 未授权辅助功能时返回明确提示，引导用户去设置页开启。
- 快捷键：macOS 默认改为 `Cmd+Option+Space`，注册时自动把 `Ctrl`→`Cmd`、`Alt`→`Option`。
- 设置页：macOS 平台显示“macOS 权限”区，可重新检测权限并一键打开系统设置；文案支持中英文。
- 打包：bundle targets 增加 `dmg`，配置 macOS `minimumSystemVersion: 10.13`。
- 需求文档：M8 标记为进行中，新增 9.13 补充项 62/63。

## 验证

- `cargo fmt --check`：通过。
- `cargo clippy --all-targets -- -D warnings`：通过。
- `cargo test`：16/16 通过。
- `npm run check`：0 错误 0 警告。
- `npm run tauri build -- --debug`：Windows 构建成功（macOS 分支在 Windows 编译为桩实现）。

## 当前状态与下一步

- M8 代码已开发（Windows 侧验证通过）；macOS 真机/CI 验证待执行，签名与公证待 Apple Developer 账号配置。
- 待用户测试建议：GitHub Actions macOS runner / Mac 云主机 / 借一台 Mac。

## 遗留问题

- macOS 分支代码无法在本机编译验证，需要在 macOS 环境确认 `AXIsProcessTrusted` 链接与权限引导交互。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
