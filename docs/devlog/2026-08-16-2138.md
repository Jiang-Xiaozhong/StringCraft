# 2026-08-16 2138 开发日志

## 机器

- Windows

## 任务

为 macOS 适配添加 GitHub Actions 云端构建验证，并整理 macOS 待办事项文档。

## 本次完成

- 新增 `.github/workflows/macos-build.yml`：
  - 推送/PR 到 `dev`、`main` 时在 `macos-latest` 上自动执行；
  - 步骤：前端依赖安装、`npm run check`、`cargo fmt --check`、clippy、test、debug 构建、上传 debug 产物（.app/.dmg）。
- 新增 `docs/MacOS待办事项.md`：
  - 已完成待验证项（权限检测、一键引导、Cmd 快捷键、托盘、DMG 配置）；
  - 待开发/待验证项（真机验证、自动更新 macOS、Universal 双架构、签名与公证、macOS 回归等）；
  - 测试方式建议（GitHub Actions / 云 Mac / 借真机）。
- 需求文档 9.13 补充项 64/65。

## 验证

- YAML 语法与文件结构已检查；Windows 构建不受影响（未改动应用代码）。

## 当前状态与下一步

- 推送到 `dev` 后 GitHub Actions 会自动在 macOS runner 上执行首次云端验证。
- M8 进行中；macOS 签名与公证待 Apple Developer 账号配置。

## 遗留问题

- workflow 首次运行结果需在 GitHub Actions 页面查看，若链接或打包问题需按日志调整。
- 本地 `.gitignore` 与 `.idea/` 的个人状态改动未纳入提交，保持现状。
