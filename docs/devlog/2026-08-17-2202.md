# 2026-08-17 2202 开发日志

## 机器

- Windows

## 任务

将仓库调整为“main 单分支开发与发布”：确认 dev 已全部合并、公开仓库、删除 dev 分支、更新开发规则。

## 本次完成

- 核对 `git log main..dev` 为空，dev 已全部合并进 main，无遗漏。
- 通过 GitHub API 将仓库 `Jiang-Xiaozhong/StringCraft` 设为公开（visibility=public）。
- 删除远端与本地 `dev` 分支。
- 更新 [AGENTS.md](../../AGENTS.md)：
  - 日常开发直接在 `main` 提交；
  - 发布直接基于 `main` 打 tag；
  - 开发阶段更新为 M1~M9。

## 关键决策

- 用户确认改为单 `main` 分支工作流，公开源码；`StringCraft-Releases` 将由用户手动删除。
- 公开仓库后，`src/assets/` 内的收款二维码会随源码公开，已按用户决策执行。

## 验证

- `git branch -a` 仅剩 `main`；
- GitHub API 返回 `private=false, visibility=public`。

## 当前状态与下一步

- 后续开发、提交、发布均在 `main` 上完成。
- macOS 真机验证、签名公证等按 `docs/MacOS待办事项.md` 推进。

## 遗留问题

- 公开仓库后收款码等资源对外可见，如需隐藏可后续处理。
