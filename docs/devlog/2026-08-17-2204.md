# 2026-08-17 2204 开发日志

## 机器

- Windows

## 任务

将检查更新地址切回主仓库（`StringCraft`，已公开），为后续在 `main` 上直接发布做准备；同步更新 README 下载链接。

## 本次完成

- `src-tauri/src/update.rs`：更新检查地址从 `StringCraft-Releases` 改回 `Jiang-Xiaozhong/StringCraft`。
- `README.md`：下载链接改为 `https://github.com/Jiang-Xiaozhong/StringCraft/releases`。

## 关键决策

- 用户后续会手动删除 `StringCraft-Releases`，为避免检查更新失效，更新地址与下载入口统一指向主仓库。

## 验证

- `cargo fmt --check` / clippy / test（16/16）通过。

## 当前状态与下一步

- 主仓库已公开且为唯一开发/发布分支（`main`）。
- 下次正式发布直接在 `main` 打 tag 并创建 Release。

## 遗留问题

- 公开仓库后收款码等资源对外可见，如需隐藏可后续处理。
