# 2026-08-17 2207 开发日志

## 机器

- Windows

## 任务

在 `main` 上发布 v0.1.3：升版本号、打包并创建 GitHub Release（主仓库）。

## 本次完成

- 版本号升至 0.1.3（package.json / Cargo.toml / tauri.conf.json / 页脚）。
- release 构建成功，安装包 3.12MB。
- 发布流程（单 main 分支）：
  - `main` 打 tag `v0.1.3` 并推送；
  - 在主仓库 `Jiang-Xiaozhong/StringCraft` 创建 Release 并上传安装包。

## 验证

- `cargo fmt --check` / clippy / test（16/16）通过；
- `npm run check` 0 错误 0 警告；
- release 构建与上传成功。

## 当前状态与下一步

- 主仓库 v0.1.3 已发布，检查更新地址指向主仓库。
- macOS 适配仍按 `docs/MacOS待办事项.md` 推进。

## 遗留问题

- 公开仓库后收款码等资源对外可见。
