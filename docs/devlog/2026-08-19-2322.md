# 2026-08-19 2322 开发日志

## 机器

- Windows

## 任务

修复“偶尔所有按钮都没反应，重启后恢复”的问题。

## 本次完成

- 根因分析：点击按钮后 `activeId` 会进入“执行中互斥”状态；若后端操作偶发长时间不返回，`activeId` 不会被清除，导致之后所有按钮点击都被忽略。
- 修复：`FloatBar.svelte` 增加点击超时兜底：
  - 点击后 8 秒内未完成则自动清除互斥状态；
  - 同时提示“操作超时，请重试”（中英文已加入语言包）；
  - 组件销毁时清理定时器。

## 验证

- `npm run check` 0 错误 0 警告；
- debug 构建成功。

## 当前状态与下一步

- 已加超时兜底，即使后端偶发卡住，按钮也能在 8 秒后恢复可用。
- 若仍偶发无响应，可继续排查后端剪贴板/键盘模拟的挂起场景。

## 遗留问题

- 超时后后台任务可能仍在运行，暂不影响 UI 使用。
