# 2026-08-22 0030 开发日志

## 机器

- Windows

## 任务

移除“自动更新”选项功能（应用内自动下载/安装更新），保留手动检查更新与自动检查更新；需求文档升级至 v0.21，并提交推送。

## 本次完成

- 需求文档 v0.21（`docs/需求方案.md`）：
  - 4.8 更名为“检查更新”，删除“自动更新（用户选择）”小节并注明 v0.21 起移除自动更新；
  - 项目概述、通用设置、非功能需求、需求清单、配置示例与字段说明、验收场景中的自动更新相关内容同步清理；
  - 9.18 第十九轮补充记录本次移除。
- 后端：
  - `config.rs`：`AppConfig` 删除 `auto_update` 字段及其默认值；
  - `update.rs`：`UpdateInfo` 删除 `asset_url`，移除 Release 资产解析、`download_update`、`launch_installer`；`check_once` 只保留自动检查并提示新版本，不再后台下载/发送 `update-ready`；
  - `commands.rs` / `lib.rs`：移除 `download_update`、`install_update` 命令及注册。
- 前端：
  - 设置页删除“自动更新”开关、`update-ready` 监听、“安装包已就绪”弹窗及相关函数；
  - `types.ts` / `defaults.ts` 删除 `autoUpdate` 字段；`api.ts` 删除 `installUpdate` 与 `UpdateInfo.assetUrl`；
  - `i18n.ts` 删除自动更新/安装相关文案。
- `README.md` / `README.en.md` 相关行、`docs/测试清单.md` 更新为“检查更新”表述。

## 关键决策

- 只移除“自动更新”（后台下载/安装安装包），保留“自动检查更新”与手动检查：自动检查只查询 GitHub Releases 并提示新版本，无下载动作，仍有用且可靠；应用内下载安装链路此前已确认不可靠，更新统一走系统浏览器打开 GitHub Releases 页面。
- 现有用户配置中的 `autoUpdate` 字段由 serde 忽略，加载不受影响，无需迁移。

## 验证

- `npm run check` 0 错误 0 警告；
- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`（16 项）全部通过；
- `npm run tauri build -- --debug` 构建成功。

## 当前状态与下一步

- 自动更新功能已移除并提交；下一步：设置页卸载功能开发；macOS 真机适配与发布（M8/M9）。

## 遗留问题

- 设置窗口长时间隐藏后存在 WebView2 被回收的潜在风险，待后续处理。
- 未打新版本号/未发布（v0.1.6 仍为当前正式版），本次为纯代码与文档变更。
