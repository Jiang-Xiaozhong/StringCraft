# 2026-08-22 0038 开发日志

## 机器

- Windows

## 任务

发布 v0.1.7：将“移除自动更新功能”随正式版发布（版本号、构建、tag、GitHub Release 与安装包上传）。

## 本次完成

- 版本号升至 0.1.7：`package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`（仅 `stringcraft` 包）、`i18n.ts` 两处页脚版本文案。
- 需求文档 v0.21 状态行更新为“v0.1.7 已发布（移除自动更新功能，保留手动检查与自动检查更新）”。
- release 构建成功：`npm run tauri build` 产出 `StringCraft_0.1.7_x64-setup.exe`（约 3.3MB，≤15MB 达标）。
- 发布流程：提交版本改动与开发日志 → 打 tag `v0.1.7` → 推送 → 通过 GitHub API 创建 Release 并上传 NSIS 安装包。
- Release 地址：https://github.com/Jiang-Xiaozhong/StringCraft/releases/tag/v0.1.7

## 关键决策

- v0.1.7 的发布内容即 2026-08-22-0030 日志中的“移除自动更新功能”变更，不夹带未验证改动。
- README 徽章为动态版本徽章（`github/v/release`），发布后自动显示 v0.1.7，无需改 README。

## 验证

- `npm run check` 0 错误 0 警告；
- `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`（16 项）全部通过；
- release 构建成功，安装包约 3.3MB。

## 当前状态与下一步

- v0.1.7 已发布；下一步：设置页卸载功能开发；macOS 真机适配与发布（M8/M9）。

## 遗留问题

- 设置窗口长时间隐藏后存在 WebView2 被回收的潜在风险，待后续处理。
- macOS 安装包需真机验证后再发布，本次仅发布 Windows 安装包。
