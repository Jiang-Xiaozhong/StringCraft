# StringCraft（字符串工坊）需求方案

> 版本：v0.4（定稿）  
> 日期：2026-08-14  
> 状态：新增按钮名称/说明与悬停说明气泡（第三轮补充），并入正文，M5 实现

---

## 1. 项目概述

StringCraft 是一款跨平台（Windows 10/11、macOS）的轻量级字符串处理工具。用户在任何应用中选中文字，通过全局快捷键或双击快捷方式呼出悬浮条，点击按钮即可对选中文本执行转换，并自动替换回原位置。

产品核心诉求：

- 双平台：Windows 10/11、macOS
- 双页面：悬浮条（常驻、全局快捷键呼入呼出）+ 设置页面
- 小巧：安装包小、内存占用低、启动快
- 纯本地：无网络请求、无遥测、不上传数据

## 2. 产品范围

### 2.1 本期包含

- 悬浮条窗口（无边框、置顶、可拖动、位置记忆）
- 全局快捷键呼入/呼出悬浮条
- 快捷方式 / 任务栏 / Dock 图标呼入悬浮条
- 自动替换：复制选中文本 → 转换 → 粘贴回去
- 20 个默认转换按钮
- 设置页：快捷键设置、按钮增删/排序/名称与说明自定义、按钮尺寸设置、行数设置
- 配置持久化（本地 JSON）
- 托盘图标（Windows 通知区域 / macOS 菜单栏）
- 占位应用图标（正式图标后续设计）
- macOS 签名与公证（已具备 Apple Developer 账号）
- Windows / macOS 双平台打包

### 2.2 本期不包含（未来可扩展）

- 正则表达式自定义转换
- 批量文件/文件夹处理
- 插件系统
- Linux 平台（后续可考虑）
- 剪贴板历史管理
- 多语言界面（v1 仅中文，英文后续）
- 自动更新（后续）
- 富文本/图片剪贴板内容的完整备份与恢复（v1 仅纯文本）

## 3. 核心交互流程

### 3.1 自动替换主流程

1. 用户在任意应用（文本编辑器、浏览器、IDE 等）中选中一段文字。
2. 用户通过以下任一方式呼出悬浮条：
   - 按全局快捷键；
   - 双击桌面/开始菜单/程序坞中的 StringCraft 快捷方式（应用已运行时，直接呼出已有悬浮条，不创建新实例）；
   - 悬浮条已常驻显示时直接使用。
3. 用户点击悬浮条上的转换按钮。
4. 应用执行：
   - 保存当前剪贴板内容（用于之后恢复；若检测到非文本内容，按 3.2 降级处理）；
   - 模拟 `Ctrl+C`（macOS 为 `Cmd+C`）复制选中文本；
   - 等待 50~100ms 后读取剪贴板；
   - 若读取结果与备份一致（说明未复制到新内容），判定为未选中文本，中止并提示（见 3.2）；
   - 对文本执行对应转换；
   - 将转换结果写入剪贴板；
   - 模拟 `Ctrl+V`（macOS 为 `Cmd+V`）粘贴替换；
   - 等待 50~100ms 后恢复原剪贴板内容。
5. 按钮给出短暂的高亮/动画反馈，表示执行完成。

### 3.2 异常与降级处理

| 场景 | 处理方式 |
| --- | --- |
| 剪贴板为空 / 未检测到选中文本 | 提示“未检测到选中文本”，恢复剪贴板，不粘贴 |
| 复制后剪贴板与备份一致 | 判定为未选中文本，提示后中止，恢复剪贴板（选中内容与原剪贴板完全相同时可能误判，可接受） |
| 目标应用不支持模拟粘贴（如终端、密码框） | 转换结果保留在剪贴板，提示“已复制到剪贴板”，由用户手动粘贴 |
| 选中文本过大（超过阈值，如 1MB） | 不执行自动替换，仅将结果放入剪贴板并提示 |
| 粘贴失败 | 不崩溃，提示失败原因，剪贴板恢复原内容 |
| 快捷键注册冲突 | 设置页给出冲突提示，要求更换组合键 |
| 剪贴板含图片/文件等非文本内容 | 放弃自动替换并提示，避免破坏用户剪贴板 |
| 目标应用以管理员身份运行（Windows） | 需 StringCraft 以管理员身份运行，否则无法注入；作为已知限制提示 |
| 配置文件损坏 | 自动备份损坏文件，恢复默认配置，不崩溃 |
| 转换结果为空字符串 | 视为正常（等于清空选中内容），正常粘贴，不提示失败 |

## 4. 功能需求

## 4.1 悬浮条

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| F1.1 | 常驻悬浮显示 | 无边框、置顶、不抢焦点；应用运行期间常驻显示（开机自启默认关闭，可在设置页开启） |
| F1.2 | 全局快捷键呼入/呼出 | 默认 `Ctrl+Alt+Space`（Win）/ `Cmd+Option+Space`（macOS），可在设置页修改 |
| F1.3 | 拖动与位置记忆 | 可拖动悬浮条；位置保存到配置，重启后恢复 |
| F1.4 | 设置按钮 | 悬浮条固定包含一个设置按钮（不可删除、不可排序），点击打开设置窗口 |
| F1.5 | 快捷按钮区 | 按钮由设置页配置：增删、排序、名称与说明自定义 |
| F1.6 | 行数 | 最多 3 行，行数在设置页配置（1~3） |
| F1.7 | 每行按钮数 | 每行最多 30 个；全部按钮最多 90 个 |
| F1.8 | 按钮尺寸 | 全局统一：宽度、高度、字号；不支持单个按钮单独调尺寸 |
| F1.9 | 超出屏幕宽度的处理 | 已确认：按钮自动缩小到最小宽度（如 36px），不做横向滚动 |
| F1.10 | 执行反馈 | 点击按钮后按钮短暂高亮，表示执行完成或失败 |
| F1.11 | 快捷方式呼入 | 双击 StringCraft 快捷方式（Windows：桌面/开始菜单 `.lnk`；macOS：`.app`）时，若应用未运行则启动并显示悬浮条；若已运行则只呼出悬浮条，不创建新实例 |
| F1.12 | 不抢焦点 | 点击悬浮条不夺取原应用焦点（Windows 使用 `WS_EX_NOACTIVATE`，macOS 使用非激活面板），保证自动替换仍作用于原应用 |
| F1.13 | 初始位置与越界归位 | 首次启动默认显示在屏幕右上角；之后记忆位置；保存的位置超出当前屏幕时自动归位到屏幕内 |
| F1.14 | 按钮名称长度 | 按钮名称建议 ≤ 8 个字符，超出时自动截断并用省略号显示；悬停气泡可查看说明或完整名称（见 F1.15） |
| F1.15 | 按钮说明气泡 | 鼠标悬停在按钮上约 500ms 后弹出气泡显示按钮说明；未填写说明时回退显示完整名称，避免名称截断看不清 |

## 4.2 设置页面

设置页为独立窗口，从悬浮条的设置按钮打开。所有修改即时生效并持久化。

### 4.2.1 快捷键设置

- 修改“呼入/呼出悬浮条”的全局快捷键；
- 支持“录制”方式设置（点击输入框后直接按目标组合键）；
- 支持恢复默认快捷键；
- 快捷键注册失败时给出冲突提示。

### 4.2.2 按钮管理

- 以列表展示全部按钮，按行分组（第 1 行 / 第 2 行 / 第 3 行）；
- 添加按钮：选择转换功能（20 个内置功能），自定义按钮名称（默认显示功能名）与按钮说明（选填）；
- 删除按钮；
- 编辑按钮名称与说明（说明选填，用于悬浮气泡展示）；
- 调整按钮顺序（上移/下移，或拖拽排序），支持跨行移动；
- 行数设置：1~3 行；
- 每行最多 30 个，超出时禁止添加并提示；
- “恢复默认按钮”一键还原 20 个内置按钮。
- 每个内置功能有固定 id（如 `upper`、`lower`），与名称/说明解耦：修改名称或说明不影响转换逻辑。

### 4.2.3 外观设置

- 按钮宽度（px）；
- 按钮高度（px）；
- 按钮字号（px）；
- 悬浮条背景不透明度（可选，默认 100%）；
- 深色/浅色模式（默认跟随系统，可手动指定）；
- 所有按钮统一变化。
- 输入范围校验：宽度 40~200px、高度 28~80px、字号 10~24px，超出时提示并取边界值。

### 4.2.4 通用设置

- 开机自启（默认关闭，可在设置页开启/关闭）；
- 剪贴板恢复开关（默认开启）；
- 失败提示方式：默认气泡提示，要求简洁、清晰、美观（悬浮条内圆角气泡，自动消失，不抢焦点）；
- 自动替换延迟（高级选项，默认值即可）。

## 4.3 默认按钮与转换规则

以下是内置的 20 个转换按钮。默认按钮名称、顺序固定；用户可删除、改名称/说明、排序。

统一约定：

- “单词”默认以空白、下划线、中横线、小数点、换行作为边界；
- 连续分隔符不做合并（已确认，如两个空格替换为两个下划线）；
- 转换后无变化时原样返回；
- 全部规则对 Unicode 文本有效（中文、英文、数字混合时行为一致）。

| # | 按钮名称 | 转换规则 | 示例 |
| --- | --- | --- | --- |
| 1 | 全大写 | 所有字母转大写 | `hello world` → `HELLO WORLD` |
| 2 | 全小写 | 所有字母转小写 | `Hello World` → `hello world` |
| 3 | 首字母大写 | 每个单词首字母大写，其余字母保持原样，分隔符保留 | `hello world` → `Hello World`；`hello_world` → `Hello_World` |
| 4 | 首字母小写 | 每个单词首字母小写，其余字母大写，分隔符保留 | `hello world` → `hELLO wORLD` |
| 5 | 句子首字母大写 | 每个句子首字母大写，其余字母小写；以 `.` `!` `?` 为句子边界 | `hello world. how are you` → `Hello world. How are you` |
| 6 | 空格→下划线 | 空格字符替换为下划线 | `hello world` → `hello_world` |
| 7 | 下划线&空格→驼峰 | 按下划线/空格分词：首词转小写，后续词首字母大写、其余小写，删除分隔符 | `hello_world`、`hello world` → `helloWorld` |
| 8 | 驼峰→下划线 | 大小写交界处分词，全部转小写，下划线连接 | `helloWorld` → `hello_world`；`HelloWorld` → `hello_world` |
| 9 | 驼峰→空格 | 大小写交界处分词，全部转小写，空格连接（已确认） | `helloWorld` → `hello world`；`HelloWorld` → `hello world` |
| 10 | 空格→中横线 | 空格替换为中横线 | `hello world` → `hello-world` |
| 11 | 下划线→中横线 | 下划线替换为中横线 | `hello_world` → `hello-world` |
| 12 | 中横线→下划线 | 中横线替换为下划线 | `hello-world` → `hello_world` |
| 13 | 下划线→空格 | 下划线替换为空格 | `hello_world` → `hello world` |
| 14 | 下划线→小数点 | 下划线替换为小数点 | `hello_world` → `hello.world` |
| 15 | 小数点→下划线 | 小数点替换为下划线 | `hello.world` → `hello_world` |
| 16 | 空格→换行 | 空格替换为换行 | `a b` → `a` + 换行 + `b` |
| 17 | 换行→空格 | 换行（含 CRLF）替换为空格 | `a` + 换行 + `b` → `a b` |
| 18 | 清除符号 | 删除除 Unicode 字母、数字、空白外的所有字符 | `hello, world!` → `hello world` |
| 19 | 清除空格 | 删除所有空格字符（半角/全角），保留换行 | `h e l l o` → `hello` |
| 20 | 清除换行 | 删除所有换行（LF/CRLF），保留空格 | `a` + 换行 + `b` → `ab` |

## 4.4 全局快捷键

- 仅支持一个“呼入/呼出”快捷键（v1）；
- 默认快捷键（已确认）：Windows `Ctrl+Alt+Space`，macOS `Cmd+Option+Space`；配置中统一用 `Ctrl` 表示主修饰键，macOS 自动映射为 `Cmd`；
- 必须包含至少一个修饰键（Ctrl / Alt / Shift / Cmd / Option 任意组合）；
- 快捷键在应用后台运行时依然有效（应用常驻后台）；
- 注册失败时提示冲突，不静默失败。

## 4.5 快捷方式呼入

- 双击 StringCraft 快捷方式（Windows：桌面/开始菜单 `.lnk`；macOS：Applications/Launchpad/Dock 中的 `.app`）即“呼入”悬浮条；
- 应用未运行时：正常启动，启动完成后直接显示悬浮条；
- 应用已运行时：第二次启动立即退出，由已运行实例呼出悬浮条（依赖单实例机制）；
- 双击快捷方式的行为固定为“呼出/显示”，不做显隐切换（已确认，避免用户无法预估状态）；
- Windows 任务栏图标、macOS Dock 图标点击时也执行相同“呼出”动作（已确认，默认开启）。

## 4.6 自动替换（核心链路）

- 平台实现：Windows 使用 SendInput；macOS 使用 CGEvent；
- macOS 首次使用时引导用户开启“辅助功能”权限，提供一键打开系统设置；
- macOS 启动时和首次使用时检测“辅助功能”权限，未授权时引导开启，设置页显示权限状态；
- Windows 无需额外权限；若目标应用以管理员身份运行，StringCraft 需以管理员身份运行（已知限制）；
- 操作完成后恢复用户原剪贴板内容（文本格式 v1 仅恢复纯文本，富文本后续；非文本内容按 3.2 降级处理）；
- 转换逻辑放在 Rust 核心层，保证跨平台行为一致，并有单元测试覆盖。
- 执行期间忽略重复点击（按钮短暂禁用，单次操作完成后恢复），防止多重粘贴；
- 转换结果允许为空字符串（等于清空选中内容），不视为失败；
- 隐私：转换文本仅在内存中处理，不写入日志、配置或磁盘。

## 4.7 托盘图标（Windows 通知区域 / macOS 菜单栏）

- 应用常驻托盘/菜单栏图标，用于快捷操作与退出；
- Windows：左键单击托盘图标呼出悬浮条；右键菜单包含“呼出悬浮条 / 打开设置 / 退出”；
- macOS：点击菜单栏图标弹出菜单，包含“呼出悬浮条 / 打开设置 / 退出”；
- 退出后：托盘图标消失，全局快捷键与自动替换全部停止。
- 右键“退出”直接退出应用，不弹二次确认（轻量工具定位，后续可加）。

## 5. 非功能需求

| 类别 | 目标 |
| --- | --- |
| 安装包体积 | Windows NSIS 安装包 ≤ 15MB（依赖系统 WebView2，缺失时引导安装 Bootstrapper，不内置固定运行时）；macOS DMG ≤ 30MB |
| 内存占用 | 空闲状态 ≤ 120MB，目标 ≤ 100MB（实测后优化） |
| 启动速度 | 冷启动 ≤ 2 秒 |
| 快捷键响应 | 按下快捷键到悬浮条显示 ≤ 200ms |
| 转换速度 | 普通文本转换 < 50ms |
| 稳定性 | 转换/粘贴失败不崩溃，均有提示 |
| 隐私 | 无网络权限、无遥测、数据不离开本机 |
| 单实例 | 重复启动不创建新实例；双击快捷方式或再次启动时，只呼出已有悬浮条 |
| 日志 | 本地调试日志（默认关闭，可开启） |
| 屏幕适配 | 适配 Windows 100%~200% DPI 缩放；悬浮条在多显示器环境下可正常显示与拖动 |
| 签名与公证 | 使用已有 Apple Developer 账号，正式版进行 macOS 签名与公证 |
| 测试范围 | 覆盖 Windows 10/11 与主流 macOS 版本，回归常用场景（记事本、浏览器、IDE、Office） |
| 平台覆盖 | Windows x64（Win10/11，arm64 后续）；macOS Intel + Apple Silicon（发布时提供 Universal 或双包） |

## 6. 技术架构

### 6.1 技术选型

- 框架：**Tauri 2**（Rust 后端 + 系统 WebView）
- 前端：**Svelte + TypeScript + Vite**（轻量、无重型运行时）
- 快捷键：`tauri-plugin-global-shortcut`
- 单实例与快捷方式呼入：`tauri-plugin-single-instance`
- 开机自启：`tauri-plugin-autostart`
- 剪贴板：`arboard` 或 `tauri-plugin-clipboard-manager`
- 键盘模拟：`enigo`（Windows SendInput / macOS CGEvent）
- 配置存储：`tauri-plugin-store`（JSON）
- 打包：Windows NSIS / MSI，macOS DMG；GitHub Actions 双平台构建

### 6.2 模块划分

```text
StringCraft
├── 前端（Svelte）
│   ├── 悬浮条窗口    // 无边框、置顶
│   └── 设置窗口      // 快捷键、按钮管理、外观
├── Rust 核心
│   ├── transform.rs  // 20 个纯函数转换 + 单元测试
│   ├── selection.rs  // 复制 → 转换 → 粘贴 → 恢复剪贴板
│   ├── hotkey.rs     // 全局快捷键注册
│   ├── config.rs     // 配置读写与校验
│   └── commands.rs   // Tauri IPC 命令
└── 平台适配
    ├── windows.rs    // SendInput
    └── macos.rs      // CGEvent + 辅助功能权限
```

### 6.3 配置数据结构（草案）

```json
{
  "version": 1,
  "hotkey": "Ctrl+Alt+Space",
  "rows": 2,
  "buttonWidth": 72,
  "buttonHeight": 32,
  "fontSize": 13,
  "opacity": 100,
  "theme": "system",
  "autoStart": false,
  "restoreClipboard": true,
  "buttons": [
    { "id": "upper", "name": "全大写", "transform": "upper", "description": "把所有字母转为大写" },
    { "id": "lower", "name": "全小写", "transform": "lower", "description": "" }
  ]
}
```

配置保存在系统应用数据目录：

- Windows：`%APPDATA%\StringCraft\config.json`
- macOS：`~/Library/Application Support/StringCraft/config.json`

## 7. 开发计划

| 阶段 | 内容 | 交付物 | 预估 |
| --- | --- | --- | --- |
| M1 | 项目骨架：Tauri 2 工程、悬浮条/设置双窗口、全局快捷键、单实例（含快捷方式二次启动呼出悬浮条）、托盘图标、占位图标接入、Windows 打包配置 | 可运行的 Windows Demo | 2~3 天 |
| M2 | 转换核心：20 个转换函数 + 单元测试 | 测试通过的转换模块 | 1~2 天 |
| M3 | 自动替换链路（Windows）：剪贴板读取、模拟粘贴、剪贴板恢复、失败提示 | Windows 全流程可用 | 2~3 天 |
| M4 | 设置页：按钮增删/排序/文字、行数、尺寸、快捷键录制、配置持久化 | 完整配置功能 | 3~4 天 |
| M5 | macOS 适配：权限引导、快捷键、DMG 打包、签名与公证（已有 Apple Developer 账号）；按钮名称/说明与悬停说明气泡（F1.15） | macOS 版本 + 按钮说明功能 | 2~3 天 |
| M6 | 打磨与发布：内存/体积优化、异常处理、双平台回归（Win10/11 + macOS 常用应用矩阵）、README | 可发布安装包 | 2~3 天 |

总计预估：**2~4 周**（1 人全职，含联调与测试）。

## 8. 已确认决策

| # | 决策点 | 确认结果 |
| --- | --- | --- |
| 1 | 转换规则细节 | “驼峰→空格”统一转小写；连续分隔符不合并；清除符号只保留字母、数字、空白 |
| 2 | 默认快捷键 | 接受：Windows `Ctrl+Alt+Space`，macOS `Cmd+Option+Space` |
| 3 | 悬浮条超宽行为 | 接受：按钮自动缩小到最小宽度（36px） |
| 4 | 悬浮条显示策略 | 常驻显示，快捷键控制显隐，不做“点击其他区域自动隐藏” |
| 5 | 托盘图标 | 显示托盘图标（Windows 通知区域 / macOS 菜单栏） |
| 6 | 开机自启 | 默认关闭，设置页可配置 |
| 7 | 剪贴板恢复 | 默认开启 |
| 8 | 失败提示 | 接受；气泡提示需简洁、明了、好看 |
| 9 | 应用图标 | 后续正式设计；先用占位图标（`assets/icon-placeholder.png`） |
| 10 | macOS 签名/公证 | 已有 Apple Developer 账号，正式发布做签名与公证 |
| 11 | 快捷方式呼入 | 双击快捷方式固定“呼出/显示”；任务栏/Dock 图标点击呼出默认开启 |
| 12 | 按钮名称与说明 | 按钮显示名称；悬停约 500ms 显示说明气泡，说明为空时回退显示完整名称；M5 实现 |

## 9. 评审补充（复查发现并已并入正文）

1. 悬浮条点击不抢焦点（F1.12）——保证自动替换仍作用于原应用；
2. 剪贴板非文本内容保护（3.2）——含图片/文件时放弃自动替换，避免破坏剪贴板；
3. Windows 管理员权限限制（3.2、4.6）——目标应用以管理员运行时，StringCraft 需同样以管理员运行；
4. 配置文件损坏容错（3.2）——自动备份并恢复默认配置；
5. 悬浮条初始位置与多显示器越界归位（F1.13）；
6. DPI 缩放适配（第 5 节）；
7. 托盘图标行为细化（4.7）；
8. 深色/浅色模式（4.2.3，默认跟随系统）；
9. 自动更新列为“本期不包含”（2.2）；
10. 双平台回归测试矩阵（第 5 节、M6）。

### 9.1 第二轮复查补充（2026-08-13，v0.3）

11. “无选中文本”判定细化：复制后剪贴板与备份一致即中止（3.1、3.2）；
12. 转换结果为空字符串视为正常清空，不误报失败（3.2）；
13. 按钮管理：排序支持跨行移动；内置功能 id 与显示文字解耦（4.2.2）；
14. 尺寸输入范围校验：宽 40~200px、高 28~80px、字号 10~24px（4.2.3）；
15. 执行期间忽略重复点击，防止多重粘贴（4.6）；
16. 隐私承诺：文本仅内存处理，不落盘、不写日志（4.6）；
17. Windows 安装包 WebView2 缺失时引导安装，控制安装包体积（第 5 节）；
18. 平台覆盖明确：Windows x64、macOS Intel + Apple Silicon（第 5 节）；
19. 托盘“退出”直接退出，不二次确认（4.7）。

### 9.2 第三轮补充（2026-08-14，v0.4）

20. 按钮名称与说明（F1.15、4.2.2、6.3）：设置页为每个按钮提供“名称 + 说明”两个字段；悬浮条显示名称，鼠标悬停约 500ms 弹出气泡显示说明（说明为空时回退完整名称），解决名称截断问题；配置字段由 `label` 调整为 `name` 并新增 `description`，M5 实现。

## 10. 核心验收场景

| 场景 | 验收标准 |
| --- | --- |
| 全局快捷键 | 在任意应用按默认快捷键，悬浮条显示/隐藏，响应 ≤ 200ms |
| 自动替换 | 在记事本/浏览器/IDE 选中文本，点击“全大写”，选中内容自动变为大写，原剪贴板内容恢复 |
| 无选中文本 | 未选中任何内容时点击按钮，弹出气泡提示，不粘贴、不改变剪贴板 |
| 按钮管理 | 设置页增删、排序、跨行移动、改名称/说明后，悬浮条即时更新且重启后保持 |
| 按钮说明气泡 | 悬停按钮约 500ms 弹出气泡显示说明；说明为空时显示完整名称 |
| 按钮尺寸 | 修改宽度/高度/字号，悬浮条全部按钮统一变化 |
| 快捷键冲突 | 注册被占用时设置页给出冲突提示，应用不崩溃 |
| 快捷方式呼入 | 应用运行时双击快捷方式，悬浮条直接呼出，不产生新实例 |
| 托盘 | 托盘左键呼出悬浮条；右键菜单可打开设置、退出 |
| 安装 | Windows NSIS / macOS DMG 安装后可正常启动，无运行时依赖报错 |
| 数据安全 | 使用后配置目录与日志中不含被转换的文本内容 |

## 11. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 部分应用不支持模拟粘贴（终端、密码框、特殊控件） | 降级为复制到剪贴板 + 气泡提示（3.2） |
| macOS 用户未授权辅助功能 | 启动/首次使用时引导，设置页显示权限状态（4.6） |
| 管理员权限应用无法注入（Windows） | 文档标注已知限制；用户需以管理员运行 StringCraft（3.2） |
| 高 DPI / 多显示器布局异常 | 逻辑像素 + 越界归位（F1.13、第 5 节） |
| WebView2 缺失（Windows 10 旧环境） | 安装包引导安装 Bootstrapper（第 5 节） |
| 剪贴板被其他程序并发改写 | 执行期间串行处理，操作前后快速读写，异常时恢复备份 |

---

需求已定稿，进入 M4/M5 开发。
